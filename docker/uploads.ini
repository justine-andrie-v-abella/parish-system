; Matches ajax/book-appointment.php's per-requirement upload flow: files up
; to 5MB each, and a submission can include several of them at once, so
; post_max_size needs real headroom above a single file's limit.
upload_max_filesize = 10M
post_max_size = 25M
memory_limit = 256M
max_execution_time = 60
